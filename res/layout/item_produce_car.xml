<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/card_outer_frame" >

    <LinearLayout
        android:id="@android:id/widget_frame"
        style="@style/card_inner_frame_insert_car" >

        <!-- suppress AndroidLintContentDescription -->

        <LinearLayout
            android:id="@+id/titleFrame"
            style="@style/card_title_frame"
            android:layout_weight="1"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/titleMakeandModel"
                style="@style/card_title" />

            <!-- suppress AndroidLintContentDescription -->

            <TextView
                android:id="@+id/contentMakeandModel"
                style="@style/card_content" />

            <!-- suppress AndroidLintContentDescription -->
        </LinearLayout>

       <!--  <ImageButton
            android:id="@android:id/button1"
            style="@style/card_popup_anchor" /> -->

    </LinearLayout>

</LinearLayout>